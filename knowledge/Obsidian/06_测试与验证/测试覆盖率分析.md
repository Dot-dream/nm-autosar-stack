# 测试覆盖率分析

> 属于 [[../00_MOC_总索引|MOC 总索引]] > **06_测试与验证**

基于 `test_nm_state.c` 的 7 个测试用例，对 NM 模块的状态机和功能路径进行覆盖率分析。

---

## 已覆盖

### OSEK Direct NM — 10 状态完整路径

| 状态 | 进入条件 | 覆盖情况 | 对应测试 |
|------|----------|:--------:|:--------:|
| `UNINIT` | `Nm_DeInit()` 调用 | 未覆盖 | — |
| `OFF` | 上电初始 | 隐式（`Test_Reset`） | 全 |
| `INIT` | `Nm_Init()` | ✅ | Test 1-7 |
| `BUSSLEEP` | INIT 完成 | ✅ | Test 1, 4 |
| `INITRESET` | NetworkRequest / 被动唤醒 | ✅ | Test 1, 3, 4 |
| `NORMAL_OPERATION` | INITRESET → TTyp 期满 | ✅ | Test 1, 2, 5 |
| `ACTIVE_WAKEUP` | CAN 控制器唤醒 | 未覆盖 | — |
| `LIMPHOME` | TMax 超时 / Error 条件 | ✅ | Test 2, 3 |
| `WAIT_BUS_SLEEP` | NetworkRelease | 未覆盖 | — |
| `PREPARE_BUS_SLEEP` | NetworkRelease（Direct） | ✅ | Test 1 |

### LimpHome 进入与恢复

| 路径 | 覆盖 | 测试 |
|------|:----:|:----:|
| `NORMAL_OPERATION → LIMPHOME`（TMax 超时, 无 Ring 消息） | ✅ | Test 2 |
| `LIMPHOME → INITRESET`（收到合法 NM PDU） | ✅ | Test 3 |

### Bus-Sleep 被动唤醒

| 路径 | 覆盖 | 测试 |
|------|:----:|:----:|
| `BUS_SLEEP → INITRESET`（收到 Alive 消息） | ✅ | Test 4 |
| `Nm_NetworkStartIndication` 回调触发 | ✅ | Test 4 |

### 通信控制

| 功能 | 覆盖 | 测试 |
|------|:----:|:----:|
| `Nm_DisableCommunication` — 停止 NM PDU 发送 | ✅ | Test 5 |
| `Nm_EnableCommunication` — 恢复 NM PDU 发送 | ✅ | Test 5 |

### 用户数据

| 功能 | 覆盖 | 测试 |
|------|:----:|:----:|
| `Nm_SetUserData` — 设置 User Data | ✅ | Test 6 |
| `Nm_GetUserData` — 读取 User Data + NodeId | ✅ | Test 6 |

### OSEK Indirect NM 基本流程

| 路径 | 覆盖 | 测试 |
|------|:----:|:----:|
| 应用消息触发进入活跃态 | ✅ | Test 7 |
| 超时无应用消息 → 休眠 | ✅ | Test 7 |

### 回调机制

| 回调 | 覆盖 | 测试 |
|------|:----:|:----:|
| `Nm_NetworkStartIndication` | ✅ | Test 4 |
| `Nm_NetworkMode` | ✅ | Test 1-5 |
| `Nm_PrepareBusSleepMode` | ✅ | Test 1 |
| `Nm_BusSleepMode` | ✅ | Test 1, 7 |
| `Nm_StateChangeNotification` | ✅ | Test 1-5 |
| `Nm_PduRxIndication` | 有实现, 未断言 | — |
| `Nm_RemoteSleepIndication` | 有实现, 未断言 | — |
| `Nm_LimpHomeIndication` | 有实现, 未断言 | — |
| `Nm_TxTimeoutException` | 有实现, 未断言 | — |

---

## 未覆盖

### AUTOSAR NM 状态机

| 缺失点 | 说明 |
|--------|------|
| `CanNm_Autosar.c` 7 状态（`NM_INIT` / `NM_NORMAL_OPERATION` / `NM_READY_SLEEP` / `NM_BUS_SLEEP` / `NM_PREPARE_BUS_SLEEP` / `NM_REPEAT_MESSAGE` / `NM_PASSIVE_MODE`） | 骨架实现，无测试用例 |
| CBV 构造与解析 | `TODO: AUTOSAR NM` 标记待实现 |
| Repeat Message 计数 | `TODO` |
| 协调器模式同步 | `TODO` |
| Partial Networking (PN) 过滤器 | `TODO` |

### CFMOTO 扩展定时器

| 缺失点 | 说明 |
|--------|------|
| `hasCfmotoExtensions == TRUE` 路径 | 测试配置中该标志始终为 `0` |
| `cfmotoTSleepRequestMin` / `cfmotoTLimpSleepReqMin` / `cfmotoTLimpHomeDTC` | 无测试驱动这些定时器 |

### Bus Load Reduction

| 缺失点 | 说明 |
|--------|------|
| `busLoadReductionActive == TRUE` 路径 | 测试配置中始终为 `0` |
| 总线负载降低算法的状态迁移 | 未覆盖 |

### 总线同步 (Bus Synchronization)

| 缺失点 | 说明 |
|--------|------|
| `busSyncEnabled == TRUE` 路径 | 测试配置中始终为 `0`, 编译开关 `NM_BUS_SYNCHRONIZATION_ENABLED` = `STD_OFF` |
| 协调器同步逻辑 | AUTOSAR NM 依赖此功能 |

### 其他未覆盖路径

| 缺失点 | 说明 |
|--------|------|
| `Nm_DeInit` 路径（`NM_DEINIT_API == STD_ON`） | 测试中该开关为 `STD_OFF` |
| `Nm_ControllerBusOff` → Bus-Off 恢复 | 无测试用例 |
| `Nm_TxConfirmation` → 发送完成确认 | 未单独测试 |
| `Nm_PassiveStartUp` → 被动启动 | 未测试 |
| `Nm_RepeatMessageRequest` / `Nm_CheckRemoteSleepIndication` | 未测试 |
| OSEK Direct 的 `ACTIVE_WAKEUP` / `WAIT_BUS_SLEEP` 独立状态 | 未精确覆盖 |
| vtable 故障注入（空指针 vtable） | 未测试 |
| 定时器池耗尽场景 | 未测试 |

---

## 覆盖率统计

| 维度 | 已覆盖 | 未覆盖 | 覆盖率 |
|------|:------:|:------:|:------:|
| OSEK Direct 10 状态 | 7 | 3 | ~70% |
| OSEK Indirect 流程 | 1 条完整路径 | 细节 | ~60% |
| AUTOSAR NM 7 状态 | 0 | 7 | 0% |
| 19 个公开 API | 11 | 8 | ~58% |
| 12 个回调 | 9 (实现) / 4 (断言) | — | — |
| 15 个编译开关 | 10 (ON) / 5 (OFF) | 5 (OFF 未测试) | ~67% |

---

## 计划扩充的测试

| 优先级 | 内容 |
|:------:|------|
| 🔴 高 | AUTOSAR NM 状态机完整测试（待骨架填充后） |
| 🟡 中 | CFMOTO 扩展定时器路径 |
| 🟡 中 | Bus Load Reduction 路径 |
| 🟢 低 | DeInit / ControllerBusOff / PassiveStartUp 路径 |
| 🟢 低 | vtable 异常注入 + 定时器池耗尽边界测试 |

---

> 7 个测试用例详情见 [[test_nm_state_7个用例详解|test_nm_state_7个用例详解]]。AUTOSAR NM 状态机骨架见 [[../03_状态机详解/AUTOSAR_NM_状态机|AUTOSAR_NM_状态机]]。
